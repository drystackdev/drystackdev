// A bar-chart glyph for the editor's "insert drawing" item - the svg node
// exists for charts and diagrams, so it reads better than a generic shape.
export const svgInsertIcon = (
  <path
    fill="none"
    stroke="currentColor"
    strokeLinecap="round"
    strokeLinejoin="round"
    strokeWidth={1.5}
    d="M3 3v18h18M7 17v-5m5 5V8m5 9v-7"
  />
);
